        +-----------+
        |   START   |
        +-----+-----+
              |
              v
    +---------+-----------+
    | getopts(set method, |
    | directory,^olume-id)|
    +---------+-----------+
              |
              v
+-------------+------------------+
|   command valid check include{ |
|   1.method valid               |
|   2.directory valid            |
|   3.EC2-BACKUP_FLAGS_SSH valid |
|   }                            |
+-------------+------------------+
              |
              v
+-------------+-----------------+
|   if -v not set, create EBS   |
|                               |
|   volume, if -v indicated,    |
|   check the size of volume    |
|                               |
+-------------+-----------------+
              |
              v
+-------------+-----------------+
|   create instance and attach  |
|   volume, mount disk          |
|                               |
+-------------------------------+
--------------+-----------------------------------------------
              v					YongCao
+-------------+-----------------+
|   execute backup              |
|   dd or rsync                 |
|   unmount and detach volume   |
|   terminal instance           |
+-------------------------------+
              |
              v
        +-----------+
        |    END    |
        +-----------+

Trap CTRL + C [Function defined]
-> Delete key pair, if created.
-> Delete the security group, if created.
-> Check if volume indicated, is attached, if yes - detach it.
-> Check if any volume created, if yes - check if it is mounted, if yes unmount it and delete it.
-> Check if any instance is running, if yes terminate it.
